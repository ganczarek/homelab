---

- hosts: rpi4
  become: true
  roles:
    - role: secured_rpi
      vars:
        secured_user: "{{ rpi4.ansible_user }}"
    - role: docker
      vars:
        docker_root: "{{ ssd_mount }}/docker"
        docker_users: ["{{ rpi4.ansible_user }}"]
